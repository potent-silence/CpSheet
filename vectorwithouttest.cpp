#include <bits/stdc++.h>
using namespace std;
typedef long long unsigned int llu;
typedef long long int lli;

int main() {
   long long int n,i;
   cin>>n;
   vector<long long int>arr;
   for(i=0;i<n;i++){
     lli temp;
     cin>>temp;
     arr.push_back(temp);//arr.emplace_back(temp);
   }
   //code goes here;
  return 0;
}
